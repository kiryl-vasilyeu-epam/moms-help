import { createStyle } from '@utils'

export const styles = createStyle({
  root: {
    w: '100%',
    h: '100vh',
    m: '0',
    p: '0',
    display: 'flex',
    flexDirection: 'column',
    minW: '320px',
    minH: '100vh',
  },
})
