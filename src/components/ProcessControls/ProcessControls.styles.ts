import { createStyles } from "@utils";

export const styles = createStyles({
  buttonRow: {
    display: "flex",
    flexDirection: "column",
    width: "100%",
    gap: "10px",
  },
  processButton: {
    flex: 1,
  },
});
