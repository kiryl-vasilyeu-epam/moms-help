import { createStyles } from "@utils";

export const styles = createStyles({
  dropdown: {
    background: "#ffffff",
    borderRadius: "8px",
    boxShadow: "0 4px 20px rgba(0, 0, 0, 0.15)",
    minWidth: "200px",
    padding: "8px 0",
    marginTop: "4px",
  },
  content: {
    padding: "8px 16px",
  },
});
