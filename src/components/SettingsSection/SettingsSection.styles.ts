import { createStyles } from '@utils';

export const styles = createStyles(() => ({
  container: {
    display: 'flex',
    flexDirection: 'column',
    flex: 1,
    width: '100%',
  },
  back: {
    width: 300,
    alignSelf: 'center',
  },
}));
