import { createSxStyles } from '@utils'

export const styles = createSxStyles({
  dialogContent: {
    p: 2,
  },
  searchField: {
    mb: 2,
  },
  list: {
    maxHeight: 350,
    overflow: 'auto',
    border: '1px solid #ddd',
    borderRadius: 1,
  },
})