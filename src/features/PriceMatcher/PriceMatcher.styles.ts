import { createStyles } from '@utils';

export const styles = createStyles({
  controls: {
    width: '100%',
    display: "flex",
    flexDirection: "column",
    maxWidth: "600px",
    minWidth: "400px",
    gap: "20px",
  }
});
